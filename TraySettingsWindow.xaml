<Window x:Class="FrozenOCR.TraySettingsWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FrozenOCR Settings"
        Width="480"
        Height="340"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Hotkey (read-only) - use theme colors so text is visible in dark theme -->
        <TextBlock Grid.Row="0" Text="Hotkey" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBox x:Name="HotkeyBox" IsReadOnly="True" Padding="8,6"
                     Background="{DynamicResource TextBox.Background}"
                     Foreground="{DynamicResource Text.Primary}"
                     BorderBrush="{DynamicResource Surface.Border}"
                     BorderThickness="1"/>
            <TextBlock Text="Change the hotkey from the in-capture settings panel." Margin="0,6,0,0" Opacity="0.75"/>
        </StackPanel>

        <!-- Screenshot folder -->
        <TextBlock Grid.Row="2" Text="Screenshot folder" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <Grid Grid.Row="3" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="ScreenshotFolderBox" Grid.Column="0" Padding="8,6" Margin="0,0,8,0"
                     VerticalContentAlignment="Center"/>
            <Button x:Name="BrowseFolderButton" Grid.Column="1" Content="Browse…" Padding="12,6" Click="BrowseFolderButton_Click"/>
            <Button x:Name="OpenFolderButton" Grid.Column="2" Content="Open" Padding="12,6" Click="OpenFolderButton_Click"/>
        </Grid>

        <!-- Theme -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" VerticalAlignment="Top">
            <TextBlock Text="Theme" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <ComboBox x:Name="ThemeCombo" Width="140" Padding="6,4" SelectionChanged="ThemeCombo_SelectionChanged"/>
        </StackPanel>

        <!-- Mouse chord -->
        <CheckBox x:Name="MouseChordCheckBox" Grid.Row="4" Content="Enable mouse chord (Ctrl + L+R mouse chord)"
                  IsChecked="True" VerticalAlignment="Top" Margin="0,36,0,0" Checked="MouseChordCheckBox_Changed" Unchecked="MouseChordCheckBox_Changed"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="OkButton" Content="OK" Width="80" Padding="8,4" Margin="0,0,8,0" Click="OkButton_Click" IsDefault="True"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="80" Padding="8,4" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
