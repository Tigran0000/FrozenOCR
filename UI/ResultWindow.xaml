<Window x:Class="FrozenOCR.UI.ResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:ClassModifier="internal"
        Title="FrozenOCR Result"
        Width="720"
        Height="420"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Recognized text"
                       FontWeight="SemiBold"/>
            <TextBlock x:Name="ProviderText"
                       FontSize="11"
                       Foreground="#666666"
                       Margin="0,4,0,8"/>
        </StackPanel>

        <TextBox x:Name="TextBox"
                 Grid.Row="1"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"/>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="Copy"
                    Width="120"
                    Margin="0,0,8,0"
                    Click="Copy_Click"/>
            <Button Content="Copy (one line)"
                    Width="140"
                    Margin="0,0,8,0"
                    Click="CopyOneLine_Click"/>
            <Button Content="Copy code block"
                    Width="140"
                    Margin="0,0,8,0"
                    Click="CopyCodeBlock_Click"/>
            <Button Content="Close"
                    Width="90"
                    Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
